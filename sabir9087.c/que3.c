#include<stdio.h>
void main()
{
    int odd ,even ;
    printf("Enter the number of elements in the array:")
    scanf("%d",&n);
    int arr[n];
    for (int i = 0 ; i < n ; i++)
    {
        printf( "Enter the element %d : ",i+1);
    }
}